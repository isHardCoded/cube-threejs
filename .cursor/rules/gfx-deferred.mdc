---
description: Deferred graphics/performance work (do not start unless asked)
globs: src/game/gfx/**,src/game/environment.js,src/game/themes/jungle.js
alwaysApply: false
---

# Graphics performance notes

## Done

- Quality tiers + Settings menu + FPS HUD
- View frustum culling (jungle `cullToCamera`)
- Godray no per-frame alloc
- **Package A (2026-07-31):**
  - Shadow caster budget (`maxShadowCasters`) — nearest/core preferred
  - View cull + shadow rank throttled to ~8 Hz
  - Adaptive Auto: DPR/AO/bloom/caster caps only — **no mid-match shadow map dispose**
  - Skip blinker updates when mesh hidden

## Still open

- Cheap phone Balanced shadows toward ~55–60 (optional)
- `cullToCamera` on other maps (cyberpunk/lava/…)
- Further microstutter profiling if still reported after Package A

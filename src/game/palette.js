// Accents for the read-anywhere bits: damage numbers, the local player marker,
// explosion rings. Softer than the old neon set on purpose — the arena is cel
// shaded now, and screaming colours on top of it just added glare.
// Map colours do not live here: see src/game/themes.
export const NEON_YELLOW = '#ffd54a'
export const NEON_CYAN = '#5fe6ff'
export const NEON_MAGENTA = '#ff4d6d'

// Cube colors are no longer assigned by id hash: every player picks a skin and
// the server sends the catalog, so see dice.js and the skin ids instead.
